# email-imap-idle
poll the email inbox and read emails

## Setup required:
put email login and IMAP server configuration in the `.env` file

## Commands to run:

```
npm install
```
```
npm start
```
